zerops:
  - setup: backend
    build:
      base: nodejs@20
      buildCommands:
        - cd backend && npm install
      deployFiles:
        - backend/~
    run:
      base: nodejs@20
      start: npm start
      ports:
        - port: 3000
          httpSupport: true